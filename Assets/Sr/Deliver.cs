using System.Collections;
using System.Collections.Generic;
using System.Runtime.ConstrainedExecution;
using UnityEngine;

public class NewBehaviourScript : MonoBehaviour
{
    private object readOnlyCollectionneedtoslowdown;
    private object debug;

    public bool The { get; private set; }
    public object package { get; private set; }

    void OnCollisionEnter2D(Collision2D other, IReadOnlyCollection readOnlyCollection)           {
        GetDebug().Log(readOnlyCollectionneedtoslowdown!);
        
    {
    
    }
     void OnCollisionEnter2D(Collision2D other)
        {
            Debug.Log("I have Collided with something")
        }
       
    {
           void OnTriggerEnter2D(Collider2D other)
            { SerializeField float(0.5fS);
                bool haspackage;false;
                bool haspackage;= true;

                    if tag(other.tag== package"costmer") && haspackage
                
                if (other.tag == "package" && !b1HasPackage)
                    Debug.Log("package pickage up.");
                b1HasPackage = true;
                SpriteRenderer.color = c1HasPackage
                Destroy(other.gameObject && !nothas package )                
                    object log = debug.log;
            if (The { } thing we; 

            object trigger = null;
            trigger;
    }
}

    private object GetDebug()
    {
        return debug;
    }

    private interface IReadOnlyCollection
    {
    }
}
